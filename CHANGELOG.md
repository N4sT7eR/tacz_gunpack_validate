# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の形式に従い、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [未リリース]

### 追加

- Lua スクリプトの構文エラーを、行・列と原因のわかる文言で報告するようになりました。
  解析ライブラリは大半の構文エラーで位置情報を捨てるため、これまで `end` の過不足や
  `then` 忘れ、テーブルのカンマ抜けなどは**行番号なし**で報告されていました
- `!=` の誤用と、条件式での `=` と `==` の取り違えを、パーサの文言ではなく
  実際の原因として報告するようになりました
- 使用できないライブラリに代替を提示するようになりました（`os` → `api:getCurrentTimestamp()`）
- `return` 忘れの報告が、スクリプトが実際に作ったテーブルの名前と宣言行を示すようになりました
- 検出できる名前に似た定数が無い場合も、次にすべきことを提示するようになりました

### 修正

- 解析ライブラリが端末へ出力していた解析器の生ログを抑止しました

## [0.11.0] - 2026-09-01

### 追加

- **Lua スクリプトの検査**（`LUA001`〜`LUA007`）。構文、未定義のグローバル、
  サンドボックス外のライブラリ、`return` 忘れ、`require` の参照切れ、文字コードを検査します。
  参照できる定数とライブラリの一覧は TaCZ 本体から読み取り、ルールファイルに記載しています
- **検出結果の分類**。すべての結果が「どの決まりに反しているか」（JSON構文 / パック構造 /
  命名規則 / スキーマ / 参照 / 翻訳 / Luaスクリプト / 推奨）を持つようになりました。
  重要度と直交する軸で、結果一覧・CSV・Markdown・JSON のすべてに出力されます
- `--category` / `--ignore-category` / `--list-categories` を追加
- GUI に分類の列と絞り込みプルダウンを追加。選択は次回起動時も保持されます
- サマリーに分類ごとの内訳を表示
- EXE のファイル名にバージョンが入るようになりました
  （`TaCZValidator-v0.11.0.exe` / `TaCZValidator-cli-v0.11.0.exe`）
- Artifact を GUI 版と CLI 版に分割。必要なほうだけダウンロードできます

### 変更

- **CSV 出力に「分類」列が `重要度` の直後に追加されました。**
  列を位置で読んでいる自動処理は影響を受けます。ヘッダ名で読んでください
- JSON 出力に `category` フィールドと `summary.by_category` を追加（既存フィールドは変更なし）
- `--severity` などで絞り込んだ場合、ファイル出力も画面と同じ内容になりました

### 修正

- ドラッグ＆ドロップされたパスの区切り文字を、Windows でネイティブ形式に正規化するようにしました

## [0.9.1] - 2026-08-31

### 変更

- GUI 版を単一ファイルではなくフォルダ構成の ZIP で配布するようにしました
- README を、ダウンロードと起動の手順から始まる構成に変更

## [0.9.0] - 2026-08-31

最初のリリース。

### 追加

- Gunpack の検証本体（JSONC パーサ、リソース索引、参照解決、検査パイプライン）
- 検査項目：JSON 構文、パック構造、命名規則、スキーマ、参照解決、翻訳キー
- コマンドライン版（`tacz-validate`）と text / CSV / Markdown / JSON 出力
- PySide6 による GUI 版。ZIP・フォルダのドラッグ＆ドロップに対応
- 英語・日本語の切り替え（初回は OS の言語を自動判定）
- ZIP を展開せずそのまま検証する機能
- Windows 向け EXE の自動ビルドとリリース

[未リリース]: https://github.com/N4sT7eR/tacz_gunpack_validate/compare/v0.11.0...develop
[0.11.0]: https://github.com/N4sT7eR/tacz_gunpack_validate/compare/v0.9.1...v0.11.0
[0.9.1]: https://github.com/N4sT7eR/tacz_gunpack_validate/compare/v0.9.0...v0.9.1
[0.9.0]: https://github.com/N4sT7eR/tacz_gunpack_validate/releases/tag/v0.9.0
